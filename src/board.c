#include "board.h"

uint8_t board[H][W];


void clear_board(void)
{
  // TODO: set all cells to zero
}

int board_in_bounds(int row, int column)
{
  // TODO: return 1 if row inside [0,H] and column inside [0,W]
  // else 0
  return 0;
}

int board_cell(int row, int column)
{
  // TODO: return -1 if out of bounds:
  // otherwise return board [row][column]
  return -1;
}
